# CoreDNS — only resolves the allowlist; logs all queries.
. {
    log
    errors
    forward . /etc/resolv.conf
    template ANY ANY {
        rcode NXDOMAIN
    }
}

. {
    log
    errors
    forward . /etc/resolv.conf
}
