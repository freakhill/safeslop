# Continuation prompt — safeslop cockpit (2026-06-21, rev 2)

Supersedes the rev-1 handoff (which set up the now-completed hostenv work). Paste the block below
into a fresh session to resume.

---

You are resuming work on safeslop (/Users/jojo/workspace/safeslop) — a single signed Go engine
+ native macOS SwiftUI app ("the cockpit", app/) that sandboxes AI coding agents in 4 isolation
tiers (host / sandbox=Seatbelt / container=Docker+squid / vm=Tart), driven by safeslop.cue, with
the GUI talking to the engine over gRPC-on-UDS.

## Operating rules
- Dev is on FORGEJO (remote `forgejo` = ssh://git@forgejojo.lucyjojo.me:2222/jojo/safeslop.git);
  GitHub (`origin`) is PAUSED — do not push there. Branch + PR; don't push main without explicit OK.
- This Claude-Code tool env's SSH_AUTH_SOCK points at the launchd agent, NOT 1Password, so
  `git push forgejo` fails publickey. Push with the 1P agent socket override:
    SSH_AUTH_SOCK="$HOME/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock" git push forgejo sp-cockpit-gui-spec
  Commits are SSH-signed via 1Password op-ssh-sign — needs 1P unlocked / `op signin`, else
  `git commit` dies with "1Password: failed to fill whole buffer". jojo's op account: BZWEAMNSWRGKPLA4VAAHVANRPM.
- jojo's shell is fish (commands HE runs must be fish); the Bash tool here is zsh-ish (bash syntax ok,
  fish `(...)` substitutions fail). Terse lowercase from jojo; `gr` = go with my #1 recommendation;
  momentum over check-ins; honesty over polish. Commit atomically, Co-Authored-By trailer. Use `ayo`
  for cross-model research, FLO (feedback-loop-optimization) for contested design decisions.

## Branch state
Active branch `sp-cockpit-gui-spec` (NOT merged to main). Tip = the FLO note commit. The FLO note +
this handoff are the UNPUSHED commits — push first (see override above). This session added, on top of
dc9f91e:
  3546396 feat(hostenv): shell-env reconstruction for the Finder-launched app (two-env firewall)
  2abe56e test(cockpit): Go control-plane smoke driver (the SwiftUI app's --audit)
  478a41a test(cockpit): Swift EngineModel/ProfileRef tests (the --mount-check)
  18f3956 test(cockpit): make cockpit-shot screenshot smoke
  6aad2aa feat(cockpit): COCKPIT_TAB deep-link so the harness can shoot any tab
  fd7179e docs(spec): GUI-flow ergonomics ayo (4-family)
  8ef2633 docs(spec): host-tier launch friction — FLO decision   <- unpushed
  (+ this handoff commit)                                          <- unpushed

## What's done + where it lives
- internal/engine/hostenv/{hostenv.go,reconstruct.go,*_test.go}: reconstructs the user's shell env
  (capture-not-parse) so the Finder-launched app finds brew/git/agents. Wired behind the two-env
  firewall: tools.DetectAll/brew/InstallByName + host/sandbox argv[0] use the rich PATH; cli.childEnv
  (internal/cli/childenv.go) overlays PATH/SHELL but drops all credentials (firewall regression test).
- The cockpit is now SELF-TESTABLE headlessly (3 layers): (1) `make check` runs internal/cli
  TestCockpitBackendSmoke — serves the REAL Control gRPC over bufconn via control.NewControlServer +
  real fns; (2) `swift test` in app/ runs app/Tests/SafeSlopCockpitTests (EngineModel state machine +
  ProfileRef), via an injected EngineClient protocol (app/Sources/.../Engine/EngineConnection.swift);
  (3) `make cockpit-shot [launch|installs|create]` (app/screenshot-cockpit.sh) launches the app,
  screencaptures the window to /tmp/safeslop-cockpit-<tab>.png, tears down — Read the PNG to SEE it
  (screencapture works from this tool env).
- specs/research/2026-06-21-gui-flow-ergonomics.md (ayo): 8 actionables S1–S7. Two load-bearing
  consensus findings: (a) honesty mandate makes dark-pattern avoidance a HARD spec (symmetric trust
  grant/revoke, show-what's-UNRESTRICTED, no stale-green-while-validating, no false-urgency counter);
  (b) the ecusson encodes DANGER LEVEL in background color ALONE — make color redundant. Free S4
  dividend: surface hostenv's resolved PATH to flag shadowed binaries.
- specs/research/2026-06-21-host-launch-friction-flo.md (FLO, 97.25/100): host-tier launch gate is
  PER-LAUNCH, never persisted (temporal/"remember" scope REJECTED — scored 4/10 anti-habituation by
  both Kimi+Gemini). Comprehension act = match THIS profile's live engine-reported risk + a guaranteed
  engine-authored cross-tier FALSE decoy; in-window phase-swap (no modal-on-modal), reuse BiometricGate
  unchanged, ONE new read RPC PreflightHostLaunch (engine authors the statements = single-source-of-
  truth); grant/revoke is risk-proportional (comprehension+Touch ID to grant; one click no biometric
  to revoke). MVP fallback if engine work slips = the Gen-0 "minimum-sufficient picker" (87.75, no new
  gRPC). The cockpit Phase state machine + BiometricGate call sites are in app/Sources/.../UI/SessionScene.swift.

## Next (jojo's call on order)
1. Push the unpushed commits to forgejo (override above). Optionally open the PR (Forgejo web UI):
   https://forgejojo.lucyjojo.me/jojo/safeslop/compare/main...sp-cockpit-gui-spec
2. Implement the FLO-selected host-launch gate (use writing-plans; needs engine PreflightHostLaunch +
   a cross-tier capability table, then the SwiftUI phase) — or ship the Gen-0 MVP picker first. Self-
   verify via make cockpit-shot + the smoke tests.
3. Other HIGH ayo actionables: ecusson non-color danger channel + show-unrestricted; "Open repo…"
   picker (the bundled app shows no profiles from Finder b/c serve cwd is /); debounced validation/no
   stale-green; engine-down graceful degradation; shadowed-binary detection from hostenv PATH; the
   AST-based New/Delete/Merge refactor (specs/0029, re-confirmed durable).
4. Carry-overs from the prior handoff: vm tier click-test, THEN `tart prune --entries caches` to
   reclaim ~28G (sonoma OCI cache; vm.go ensureBase is idempotent so it won't re-pull while the local
   safeslop-vm-base template exists); sign+notarize the .app; merge sp-cockpit-gui-spec → forgejo main.

## Verify before "done"
make check ; make build ; fish tests/run.fish ; fish scripts/slop-sync-help.fish check ;
fish scripts/slop-pinning.fish ; (in app/) swift build && swift test ; make cockpit-shot
