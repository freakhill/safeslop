# Continuation prompt — safeslop cockpit (2026-06-21)

Paste the block below into a fresh session to resume.

---

You are resuming work on **safeslop** (`/Users/jojo/workspace/safeslop`) — a single signed Go engine
+ native macOS SwiftUI app ("the cockpit", `app/`) that sandboxes AI coding agents in 4 isolation
tiers (host / sandbox=Seatbelt / container=Docker+squid / vm=Tart), driven by `safeslop.cue`.

## Where things stand
- **Dev is on Forgejo** (`forgejo` remote), GitHub paused — do NOT push to `origin`/GitHub, and don't
  push `main` without explicit OK (branch + PR). jojo uses **fish**; commands he runs must be fish.
- **forgejo `main`** (tip `71fd320`): host/sandbox/container tiers all work end-to-end through the
  cockpit (sandbox tty-ioctl fix, container tier-aware shell + open-egress DNS). vm tier: image
  (`safeslop-vm-base`) is pulled but NOT click-tested yet.
- **Active branch `sp-cockpit-gui-spec`** (pushed to forgejo, ~14 commits, **NOT merged to main**) — the
  GUI Phase 1. All gated green (`make check`, `swift build`, fish suite, app mounts). Contains:
  - 3-tab cockpit (Launch / Installs / Create) over a shared `@Observable EngineModel`.
  - **Installs**: per-tool, brew-aware, **non-clobbering** catalog (`internal/engine/tools`) — detects
    present/source, only offers *missing* tools, instant "?" first-paint then detection. Catalog incl.
    runtimes, languages (go/rust/swift/clojure/babashka/lean4/xcode), forge CLIs (git/gh/tea),
    containers, secret managers, agents. Install routes verified via `brew info`.
  - **Arbiter** (`policy.RiskSummary`): break-glass consequences (not a score) on Launch rows + trust
    sheet; danger shown via a colored **ecusson** (RiskBadge), icon+text agree.
  - **Create tab**: text-canonical live CUE editor (`ValidatePolicy` RPC + `policy.LoadBytes`), live
    arbiter preview, **file-scope drag-drop helper** (generates `files:` block, merges into a profile),
    **profile New/Delete** (targeted text edits), **Presets** menu = bundled stdlib
    (`internal/engine/policy/presets/*.cue`, `ListPresets` RPC).
  - **File scope**: CUE `files:{read,write,deny}` + sandbox enforcement + **auto-deny-secrets** (curated
    home credential stores; SSH *keys* not the dir; EXCLUDES ambiguous bucket ~/.npmrc/.kube/.docker/
    .gitconfig/.git/.env; only with a granted scope; explicit grant opts out).
  - **App bundle**: `make cockpit-app` → self-contained `SafeSlop.app` (icon `app/packaging/`,
    `make-icon.swift` = green shield + `>_`; engine bundled at Contents/MacOS/safeslop via
    `EngineBinary.resolved`). `make cockpit` = dev click-test harness (seeds a repo + serves).
  - **CLI**: `safeslop launch <profile> --config <dir>` for hotkeys (`launchWorkspace`, canonicalized).

## THE NEXT TASK (designed, not yet built)
Implement **`internal/engine/hostenv`** per `specs/research/2026-06-21-gui-shell-env.md`: a Finder-
launched `.app` gets a minimal env, so the engine can't find brew/mise/agents/shims. Reconstruct the
user's shell env by *running their shell* (`$SHELL -ilc` resolved via `dscl`, marker-delimited, stdin
/dev/null, TERM=dumb, timeout, fish branch; fallbacks `path_helper -s` + hardcoded brew/user/shim dirs;
sanitize DYLD_*/world-writable/`..`/multiline). Use it ONLY for host-side discovery (`tools.DetectAll`
LookPath) + binary resolution for spawn — the **two-env firewall**: rich `host_discovery_env` never
crosses into the sandbox; the existing `childEnv` allowlist remains the sole gate. Add a regression test
asserting the sandbox env stays scrubbed even when hostenv is rich. Surfaces + actionables are in the
design note.

## Other open items (jojo's call on order)
- **"Open repo…" picker** in the cockpit (multi-repo) — the bundled app shows "no profiles" from Finder
  because serve's cwd is `/`. Uses the existing `ListProfiles(configPath:)`. Makes the standalone app usable.
- **vm tier click-test** (image ready) + vm/container **file-scope** support.
- **Sign + notarize** the .app for distribution (mirror `~/workspace/claudemetre` sign.sh/release.sh).
- The **FLO hand-offs** in specs/0029 (visual-authoring line; wizard-vs-editor) — the New/Delete/Merge
  text edits are targeted/fragile; they become robust AST ops once that's decided.
- **Merge `sp-cockpit-gui-spec` → forgejo main** when jojo signs off.

## Working style
Terse lowercase from jojo; `gr` = go with my #1 recommendation; momentum over check-ins; honesty over
polish. Use `ayo` (cross-model: Gemini via or_ask, GLM, Kimi) for design research; verify real exit
codes (not `cmd | tail`). Commit atomically; end commits with the Co-Authored-By trailer.
