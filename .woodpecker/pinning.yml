# Version-pinning gate (no `latest` in pinned files). Native on darwin/arm64 (host fish).
when:
  - event: push
    branch: main
  - event: pull_request
labels:
  platform: darwin/arm64
steps:
  - name: pinning
    commands:
      - fish scripts/slop-pinning.fish
